---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span
  class={`rounded-full border px-4 py-2 text-sm whitespace-nowrap
          bg-elevated text-secondary border-border
          ${className}`}
>
  <slot />
</span>
