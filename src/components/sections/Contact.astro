---
import MessageIcon from '@/components/icons/MesageIcon.astro';
import SectionTitle from '@/components/ui/SectionTitle.astro';
import Button from '@/components/ui/Button.astro';
import FormField from '@/components/ui/FormField.astro';
import FormTextArea from '@/components/ui/FormTextArea.astro';

import backgroundImage from '@/assets/images/background-cta.webp';
---

<section
  id="contact"
  class="grid scroll-mt-24 grid-cols-1 items-stretch gap-6 lg:grid-cols-2 lg:gap-12"
>
  <div
    class="group relative flex min-h-87.5 items-center justify-center overflow-hidden rounded-3xl p-8 md:p-12"
    data-aos="fade-right"
  >
    <div
      class="absolute inset-0 z-0 bg-cover bg-center transition-transform duration-700 group-hover:scale-110"
      style={`background-image: url(${backgroundImage.src});`}
    >
    </div>
    <div class="absolute inset-0 z-10 bg-indigo-500/60"></div>
    <!-- Contenido  -->
    <div class="relative z-20 flex flex-col items-center text-center">
      <div
        class="mb-6 flex size-20 items-center justify-center rounded-2xl border border-white/20 bg-white/10 shadow-2xl"
      >
        <MessageIcon class="size-10 text-white" />
      </div>

      <h2 class="mb-4 text-3xl font-bold text-white md:text-4xl">
        ¿Hablamos de tu próximo proyecto?
      </h2>
      <p class="max-w-md text-lg leading-relaxed text-white">
        Ya sea que tengas una idea clara o estés empezando, me encantaría escucharte y ver cómo
        podemos colaborar
      </p>
    </div>
  </div>

  <div class="bg-surface border-border/50 rounded-3xl border p-4 md:p-8" data-aos="fade-left">
    <div class="mb-6">
      <SectionTitle>Contáctame</SectionTitle>
      <p class="text-secondary mt-4">
        Cuéntame un poco más sobre lo que necesitas y me pondré en contacto contigo pronto
      </p>
    </div>

    <form class="flex flex-col gap-4" action="https://formspree.io/f/xykkkqbg" method="POST">
      <input type="text" name="_gotcha" style="display:none" />
      <FormField label="Nombre completo" name="name" placeholder="Jhon Doe" autocomplete="name" />
      <FormField
        label="Email"
        type="email"
        name="email"
        placeholder="jhondoe@gmail.com"
        autocomplete="email"
      />
      <FormField label="Asunto" name="subject" placeholder="¿En qué puedo ayudarte?" />
      <FormTextArea
        label="Mensaje"
        name="message"
        placeholder="Cuéntame más sobre tu proyecto..."
      />
      <div>
        <Button type="submit" fullWidth class="md:w-auto">Enviar Mensaje</Button>
      </div>
    </form>
  </div>
</section>
