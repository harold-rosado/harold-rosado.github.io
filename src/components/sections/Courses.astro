---
import SectionTitle from '../ui/SectionTitle.astro';
import Button from '@/components/ui/Button.astro';

import iconBootstrap from '@/assets/illustrations/bootstrap.svg';
import iconGit from '@/assets/illustrations/git.svg';
import iconFigma from '@/assets/illustrations/figma.svg';

const courses = [
  {
    title: 'Bootstrap desde cero',
    description:
      'Aprende a construir interfaces responsivas y consistentes, desde las bases hasta layouts reales con el framework CSS más popular.',
    icon: iconBootstrap.src,
    url: 'https://www.youtube.com/watch?v=gWan3LptzgM',
    label: 'Aprender ahora',
    status: 'available',
  },
  {
    title: 'Introducción a Git',
    description:
      'Domina los fundamentos de Git y el control de versiones para trabajar de forma ordenada y colaborativa en cualquier equipo de desarrollo.',
    icon: iconGit.src,
    url: 'https://www.udemy.com/course/...',
    label: 'Próximamente',
    status: 'coming-soon',
  },
  {
    title: 'Prototipado en Figma',
    description:
      'Diseña y prototipa interfaces en Figma, aplicando principios de UX/UI y flujos de usuario complejos para testing y validación.',
    icon: iconFigma.src,
    url: '#',
    label: 'Próximamente',
    status: 'coming-soon',
  },
];
---

<section
  id="courses"
  class="bg-surface border-border/50 scroll-mt-24 rounded-3xl border p-6 md:p-14"
>
  <div class="mb-8 flex flex-col items-center text-center" data-aos="fade-up">
    <SectionTitle>Cursos y Talleres</SectionTitle>
    <p class="text-secondary mt-2 text-lg leading-relaxed text-balance">
      A lo largo de mi carrera, he tenido la oportunidad de compartir mis conocimientos a través de
      cursos y talleres enfocados en <span class="text-primary font-medium"
        >UX/UI y desarrollo Frontend</span
      >.
    </p>
  </div>

  <div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
    {
      courses.map((course, index) => (
        <article
          class="group border-border bg-elevated/30 flex flex-col rounded-2xl border p-6 transition-all duration-300 hover:-translate-y-1 hover:shadow-xl hover:shadow-indigo-500/5"
          data-aos="fade-up"
          data-aos-delay={index * 150}
        >
          <div class="bg-surface border-border group-hover:border-accent mb-4 flex size-18 items-center justify-center rounded-2xl border transition-colors">
            <img
              src={course.icon}
              alt={`Icono de ${course.title}`}
              class="size-14 object-contain transition-transform group-hover:scale-105"
            />
          </div>

          <h5 class="text-primary mb-2 text-xl font-bold">{course.title}</h5>

          <p class="text-secondary mb-6 line-clamp-3 flex-1 leading-relaxed">
            {course.description}
          </p>

          <div class="mt-auto flex flex-col md:items-start">
            {course.status === 'available' ? (
              <a href={course.url} target="_blank" rel="noopener noreferrer">
                <Button variant="filled" class="w-full md:w-auto">
                  {course.label}
                </Button>
              </a>
            ) : (
              <Button variant="outlined" class="w-full md:w-auto" disabled>
                {course.label}
              </Button>
            )}
          </div>
        </article>
      ))
    }
  </div>
</section>
